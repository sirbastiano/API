# This file is generated by SciPy's build process
# It contains system_info results at the time of building this package.
from enum import Enum

__all__ = ["show"]
_built_with_meson = True


class DisplayModes(Enum):
    stdout = "stdout"
    dicts = "dicts"


def _cleanup(d):
    """
    Removes empty values in a `dict` recursively
    This ensures we remove values that Meson could not provide to CONFIG
    """
    if isinstance(d, dict):
        return { k: _cleanup(v) for k, v in d.items() if v != '' and _cleanup(v) != '' }
    else:
        return d


CONFIG = _cleanup(
    {
        "Compilers": {
            "c": {
                "name": "gcc",
                "linker": r"ld64",
                "version": "14.2.0",
                "commands": r"/opt/homebrew/bin/gcc-14",
                "args": r"-I/opt/homebrew/opt/openblas/include",
                "linker args": r"-L/opt/homebrew/opt/openblas/lib, -I/opt/homebrew/opt/openblas/include",
            },
            "cython": {
                "name": r"cython",
                "linker": r"cython",
                "version": r"3.0.11",
                "commands": r"cython",
                "args": r"",
                "linker args": r"",
            },
            "c++": {
                "name": "gcc",
                "linker": r"ld64",
                "version": "14.2.0",
                "commands": r"/opt/homebrew/bin/g++-14",
                "args": r"-I/opt/homebrew/opt/openblas/include",
                "linker args": r"-L/opt/homebrew/opt/openblas/lib, -I/opt/homebrew/opt/openblas/include",
            },
            "fortran": {
                "name": "gcc",
                "linker": r"ld64",
                "version": "14.2.0",
                "commands": r"/opt/homebrew/opt/gcc/bin/gfortran",
                "args": r"",
                "linker args": r"-L/opt/homebrew/opt/openblas/lib",
            },
            "pythran": {
                "version": r"0.17.0",
                "include directory": r"../../../pip-build-env-cavr7ust/overlay/lib/python3.10/site-packages/pythran"
            },
        },
        "Machine Information": {
            "host": {
                "cpu": r"aarch64",
                "family": r"aarch64",
                "endian": r"little",
                "system": r"darwin",
            },
            "build": {
                "cpu": r"aarch64",
                "family": r"aarch64",
                "endian": r"little",
                "system": r"darwin",
            },
            "cross-compiled": bool("False".lower().replace('false', '')),
        },
        "Build Dependencies": {
            "blas": {
                "name": "openblas",
                "found": bool("True".lower().replace('false', '')),
                "version": "0.3.29",
                "detection method": "pkgconfig",
                "include directory": r"/opt/homebrew/Cellar/openblas/0.3.29/include",
                "lib directory": r"/opt/homebrew/Cellar/openblas/0.3.29/lib",
                "openblas configuration": r"USE_64BITINT= DYNAMIC_ARCH=1 DYNAMIC_OLDER= NO_CBLAS= NO_LAPACK= NO_LAPACKE= NO_AFFINITY=1 USE_OPENMP=1 VORTEX MAX_THREADS=56",
                "pc file directory": r"/opt/homebrew/opt/openblas/lib/pkgconfig",
            },
            "lapack": {
                "name": "openblas",
                "found": bool("True".lower().replace('false', '')),
                "version": "0.3.29",
                "detection method": "pkgconfig",
                "include directory": r"/opt/homebrew/Cellar/openblas/0.3.29/include",
                "lib directory": r"/opt/homebrew/Cellar/openblas/0.3.29/lib",
                "openblas configuration": r"USE_64BITINT= DYNAMIC_ARCH=1 DYNAMIC_OLDER= NO_CBLAS= NO_LAPACK= NO_LAPACKE= NO_AFFINITY=1 USE_OPENMP=1 VORTEX MAX_THREADS=56",
                "pc file directory": r"/opt/homebrew/opt/openblas/lib/pkgconfig",
            },
            "pybind11": {
                "name": "pybind11",
                "version": "2.13.6",
                "detection method": "config-tool",
                "include directory": r"unknown",
            },
        },
        "Python Information": {
            "path": r"/Users/paramthind/Desktop/API/sklearn-env/bin/python",
            "version": "3.10",
        },
    }
)


def _check_pyyaml():
    import yaml

    return yaml


def show(mode=DisplayModes.stdout.value):
    """
    Show libraries and system information on which SciPy was built
    and is being used

    Parameters
    ----------
    mode : {`'stdout'`, `'dicts'`}, optional.
        Indicates how to display the config information.
        `'stdout'` prints to console, `'dicts'` returns a dictionary
        of the configuration.

    Returns
    -------
    out : {`dict`, `None`}
        If mode is `'dicts'`, a dict is returned, else None

    Notes
    -----
    1. The `'stdout'` mode will give more readable
       output if ``pyyaml`` is installed

    """
    if mode == DisplayModes.stdout.value:
        try:  # Non-standard library, check import
            yaml = _check_pyyaml()

            print(yaml.dump(CONFIG))
        except ModuleNotFoundError:
            import warnings
            import json

            warnings.warn("Install `pyyaml` for better output", stacklevel=1)
            print(json.dumps(CONFIG, indent=2))
    elif mode == DisplayModes.dicts.value:
        return CONFIG
    else:
        raise AttributeError(
            f"Invalid `mode`, use one of: {', '.join([e.value for e in DisplayModes])}"
        )
